import { createAction } from "typesafe-actions";

/**
 *  TODO:
 *  - Add payload types
 */

/** @type { ActionCreator<'SET_CURRENT_PAGESTATE'> } */
export const setCurrentPageState = createAction("SET_CURRENT_PAGESTATE")();
