/**
 * TODO:
 * type returns and state
 */

export const getEncoderRotation = (state) => state.animations.encoderRotation;

export const getDeviceRotation = (state) => state.animations.deviceRotation;
